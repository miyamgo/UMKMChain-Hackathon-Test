# .env has been sanitized in this repository to avoid leaking secrets.
# If you need to run locally, copy `.env.example` to `.env` and fill the values.
# PRIVATE_KEY and RPC URLs must be provided locally and must never be committed.